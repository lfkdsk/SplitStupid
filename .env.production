# Build-time env for production. Vite loads this automatically when
# running `vite build`. All values are public — the OAuth client_id is
# meant to travel in the authorize URL, the worker URLs are public.
# Safe to commit.
VITE_OAUTH_CLIENT_ID=Ov23liCg29llKxJ7b0jv
VITE_OAUTH_WORKER_URL=https://auth.lfkdsk.org/splitstupid
VITE_API_URL=https://api.splitstupid.lfkdsk.org
