// @splitstupid/core — the isomorphic half of SplitStupid. Everything here
// is pure TypeScript with no DOM, React, or platform globals beyond fetch,
// so the web app (repo root) and the React Native app (mobile/) import the
// exact same logic and stay in lock-step.

export * from './types'
export * from './settle'
export * from './api'
export * from './me'
export * from './avatar'
