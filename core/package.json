{
  "name": "@splitstupid/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Platform-agnostic SplitStupid logic — data shapes, settlement math, and the Worker API client. Imported by both the web app (root) and the React Native app (mobile/). No DOM, no React, no platform globals beyond fetch.",
  "main": "./src/index.ts",
  "module": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "typescript": "~6.0.2",
    "vitest": "^4.1.9"
  }
}
