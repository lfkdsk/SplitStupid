{
  "name": "splitstupid-data",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "db:migrate": "wrangler d1 migrations apply splitstupid --remote",
    "db:migrate:local": "wrangler d1 migrations apply splitstupid --local"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250101.0",
    "typescript": "~6.0.2",
    "wrangler": "^4.0.0"
  }
}
